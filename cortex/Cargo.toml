[package]
name = "cortex"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Universal local transformer engine with persistent memory. Runs ternary, quantized, and float models from GGUF."
keywords = ["transformer", "inference", "llm", "ternary", "memory"]
categories = ["science", "mathematics"]

[features]
default = ["gpu"]
gpu = ["wgpu", "pollster", "bytemuck", "half", "vram-heap"]
# memory = ["engram"]  # disabled until engram lands on this machine

[dependencies]
thiserror = "2"
tracing = "0.1"
rayon = "1"
# wgpu upgraded 24 → 29. default-features = false drops the DX12 + GLES
# backends (which pull a windows-rs version conflict in wgpu 29 on
# Windows); cortex uses Vulkan on the dev box. DX12/GLES support is a
# post-migration concern for AMD-on-Windows users.
wgpu = { version = "29", default-features = false, features = ["vulkan", "wgsl"], optional = true }
pollster = { version = "0.4", optional = true }
bytemuck = { version = "1", features = ["derive"], optional = true }
half = { version = "2", optional = true }
# Substrate-owned VRAM allocator. Bump-arena heaps over wgpu buffers
# that cortex pre-allocates at startup and sub-allocates per call,
# avoiding the per-dispatch device.create_buffer churn that hit
# wgpu-29's allocator pressure ceiling. See C:\src\vram-heap\DESIGN.md
# for the architectural commitment.
vram-heap = { path = "../../vram-heap", optional = true }
# engram lives on the old machine; temporarily disabled until it's copied over or absorbed into cortex per the roadmap.
# engram = { path = "../../engram", default-features = false, optional = true }

[dev-dependencies]
tempfile = "3"
